---
- hosts: all
  name: Setup base image
  become: yes
  become_method: sudo
  roles:
    - automated_security_updates
    - banner
    - clamav
    - devops
    - freeipa_client
    - htop
    - joiner
    - persist_journald
